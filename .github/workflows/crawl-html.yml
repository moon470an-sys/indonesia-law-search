name: Crawl JDIH HTML sites (kemnaker / kemenpppa / brin / pkp)

on:
  workflow_dispatch:
    inputs:
      sites:
        description: "Site keys (space-separated). Empty = all."
        required: false
        default: "kemnaker kemenpppa brin pkp"
      max_pages:
        description: "Max pages per site"
        required: false
        default: "200"

permissions:
  contents: write

concurrency:
  group: crawl-html
  cancel-in-progress: true

jobs:
  crawl:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: crawler/requirements.txt

      - name: Install deps
        working-directory: crawler
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run HTML scrapers
        run: |
          python -m crawler.html_scrapers ${{ inputs.sites }} --max-pages ${{ inputs.max_pages }}

      - name: List output
        run: |
          ls -la data/raw/ || true
          for f in data/raw/jdih_*.jsonl; do
            [ -e "$f" ] || continue
            echo "$f → $(wc -l < "$f") rows"
            echo "  head:"; head -1 "$f"
          done

      - name: Upload artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: html-jsonl
          path: data/raw/jdih_*.jsonl
          retention-days: 14
          if-no-files-found: ignore
